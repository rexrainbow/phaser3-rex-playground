import GetLocateText from './GetLocateText.js';

var GetInfoDialogText = function (scene, label, locate) {
    return GetLocateText(scene, 'InfoDialog', label, locate);
}

export default GetInfoDialogText;